# Stage 14 Web Portal Source Map

| Source | Disposition |
|---|---|
| `apps/web-static/ompt-reference/styles.css` | Authoritative visual tokens and layout primitives imported by both builds |
| `apps/web-static/ompt-reference/index.html` | Documentation hero and terminal composition reference |
| `apps/web-static/ompt-reference/architecture.html` | Architecture flow and timeline reference |
| `apps/web-static/ompt-reference/publications.html` | DOI filtering and record-card reference |
| `apps/web-static/ompt-reference/repository.html` | Proof-console and evidence-card reference |

The React applications are new implementations. Static content that claimed
unverified repository or production state was not carried forward. Live API
failures render as unavailable states instead of fabricated proof.
