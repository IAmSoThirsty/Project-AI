# (Substrate Expansion Vector)             [2026-04-09 04:26]
#                                          Status: Active
# Optional runtime dependencies for feature-complete recovery.
# Base install remains in requirements.txt; use this file when you want the
# optional video, audio, and Redis-backed paths enabled in the current env.

opencv-python-headless
openai-whisper
pydub
redis

# Blockchain anchoring (Chainlink integration)
web3>=6.0.0  # Ethereum/EVM blockchain interaction

# HSM Integration (Hardware Security Module)
# python-yubihsm>=2.0.0  # YubiHSM 2 support (uncomment if using YubiHSM)
# boto3>=1.34.24  # AWS CloudHSM support (already in main requirements.txt)

