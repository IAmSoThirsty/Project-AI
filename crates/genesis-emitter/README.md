# Project-AI Genesis Emitter

This Rust crate canonicalizes an evidence event, binds it to the prior hash,
and emits a verifiable record. It does not evaluate governance, grant
authority, or execute an event. Cargo uses the SemVer-compatible development
form `0.0.0-dev0`; package metadata records the repository's canonical
development version `0.0.0.dev0`.
