#                                           [2026-03-05 09:28]
#                                          Productivity: Active
name: "CodeQL Analysis"

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]
  schedule:
    - cron: "27 3 * * 1" # Weekly Monday 3:27 AM UTC

permissions:
  actions: read
  contents: read
  security-events: write

jobs:
  analyze:
    name: Analyze (${{ matrix.language }})
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        language: [python, javascript]
        include:
          - language: python
            build-mode: none
          - language: javascript
            build-mode: none
    steps:
      - name: Checkout repository
        uses: 34e114876b0b11c390a56381ad16ebd13914f8d5  # pinned from actions/checkout@v4

      - name: Initialize CodeQL
        uses: 3b1a19a80ab047f35cbb237b5bd9bdc1e14f166c  # pinned from github/codeql-action/init@v3
        with:
          languages: ${{ matrix.language }}
          build-mode: ${{ matrix.build-mode }}
          queries: security-extended,security-and-quality

      - name: Perform CodeQL Analysis
        uses: 3b1a19a80ab047f35cbb237b5bd9bdc1e14f166c  # pinned from github/codeql-action/analyze@v3
        with:
          category: "/language:${{ matrix.language }}"
