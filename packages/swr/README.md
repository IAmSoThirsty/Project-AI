# Project-AI Sovereign War Room

SWR defines deterministic scenarios across five rounds and scores supplied decisions without side
effects. Recording a result is an actuation and therefore requires `ExecutionGate` governance plus
an exact scoped capability (`swr.scenario.record`, `swr:<scenario-id>`). The legacy bundle,
deployment, and duplicate governance surfaces remain provenance inputs rather than runtime paths.
