"""
sovereign_vault — object-level narrow-release vault architecture.

No "unlock." No "mount." No "session."
Only: verify -> authorize -> release -> zeroize.

See README.md for the VERIFIED / SEAM / LIMITATION register mapped
against each blind spot this package addresses.
"""

__version__ = "0.1.0"
