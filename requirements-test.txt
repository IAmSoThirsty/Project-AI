# Test Dependencies - Complete Suite
# Install with: pip install -r requirements-test.txt

# Core test framework (already in requirements.txt)
pytest>=7.4.4
pytest-asyncio>=0.23.3
pytest-cov>=4.1.0

# Data science libraries
numpy>=1.24.0
pandas>=2.0.0
scipy>=1.10.0
scikit-learn>=1.3.0

# Property-based testing
hypothesis>=6.80.0

# Optional features
opencv-python-headless>=4.8.0  # Image processing tests
openai-whisper>=20231117  # Audio processing tests
pydub>=0.25.0  # Audio manipulation

# External service clients
redis>=5.0.0  # Redis integration tests
temporalio>=1.0.0  # Temporal workflow tests (if available)

# Additional test utilities
faker>=18.0.0  # Generate fake data for tests
freezegun>=1.2.0  # Mock datetime in tests
responses>=0.23.0  # Mock HTTP responses
aioresponses>=0.7.0  # Mock async HTTP

# Type checking (dev)
mypy>=1.8.0
types-requests
types-redis

# Note: Some tests may require external services running:
# - Redis server (docker run -d -p 6379:6379 redis)
# - Temporal server (for workflow tests)
# - PostgreSQL/other databases (for integration tests)
