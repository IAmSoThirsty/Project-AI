# Installation Requirements

# Core dependencies
fastapi==0.109.1
uvicorn[standard]==0.27.0
pydantic==2.5.3
python-dotenv==1.0.0

# Web server (production)
gunicorn==22.0.0

# Database
sqlalchemy==2.0.25
alembic==1.13.1
# Note: sqlite3 is part of Python's standard library

# Security
python-multipart==0.0.22
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
cryptography==42.0.4
asn1crypto>=1.5.1  # RFC 3161 TSA support

# HTTP client
httpx==0.26.0
requests==2.31.0
rfc3161ng>=2.1.2
pyasn1>=0.4.8

# Save points system dependencies
# Note: uses Python standard library only

# Testing
pytest==7.4.4
pytest-asyncio==0.23.3
pytest-cov==4.1.0

# Development
black==24.1.1
flake8==7.0.0
mypy==1.8.0

# Legion integration
# Note: uses existing dependencies listed above

# Optional: Model serving
# torch==2.1.2
# transformers==4.37.0
# accelerate==0.26.0

# ATLAS Ω Constitutional Engine Dependencies
cbor2>=5.6.0  # Canonical binary encoding for hash integrity (updated for security)
jsonschema>=4.23.0  # Schema validation (updated for security)
pyyaml>=6.0.2  # Configuration loading (updated for security)

# Constitutional Sovereignty - Off-Machine Backup (VECTOR 5, 10, 11)
ipfshttpclient==0.8.0a2  # IPFS distributed immutable storage
boto3==1.34.24  # AWS S3 with WORM (Write-Once-Read-Many) object lock

# Monitoring and Metrics
prometheus-client>=0.18.0  # Metrics export and monitoring
grafana-client>=5.0.0  # Grafana API client for dashboard management
