apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: The Obsidian Protocol
  namespace: default
  labels:
    app: The Obsidian Protocol
    release: prometheus
spec:
  selector:
    matchLabels:
      app: The Obsidian Protocol
  endpoints:
  - port: http
    path: /metrics
    interval: 30s
    scrapeTimeout: 10s
