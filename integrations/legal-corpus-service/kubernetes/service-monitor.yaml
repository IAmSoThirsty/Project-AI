apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: Legal Corpus Service
  namespace: default
  labels:
    app: Legal Corpus Service
    release: prometheus
spec:
  selector:
    matchLabels:
      app: Legal Corpus Service
  endpoints:
  - port: http
    path: /metrics
    interval: 30s
    scrapeTimeout: 10s
