apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: Cognitive Load Balancer
  namespace: default
  labels:
    app: Cognitive Load Balancer
    release: prometheus
spec:
  selector:
    matchLabels:
      app: Cognitive Load Balancer
  endpoints:
  - port: http
    path: /metrics
    interval: 30s
    scrapeTimeout: 10s
