apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: World History Service
  namespace: default
  labels:
    app: World History Service
    release: prometheus
spec:
  selector:
    matchLabels:
      app: World History Service
  endpoints:
  - port: http
    path: /metrics
    interval: 30s
    scrapeTimeout: 10s
