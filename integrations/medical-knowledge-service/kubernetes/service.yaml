apiVersion: v1
kind: Service
metadata:
  name: Medical Knowledge Service
  namespace: default
  labels:
    app: Medical Knowledge Service
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: http
    protocol: TCP
    name: http
  selector:
    app: Medical Knowledge Service
  sessionAffinity: None
