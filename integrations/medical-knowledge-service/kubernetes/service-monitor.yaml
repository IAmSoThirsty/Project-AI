apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: Medical Knowledge Service
  namespace: default
  labels:
    app: Medical Knowledge Service
    release: prometheus
spec:
  selector:
    matchLabels:
      app: Medical Knowledge Service
  endpoints:
  - port: http
    path: /metrics
    interval: 30s
    scrapeTimeout: 10s
