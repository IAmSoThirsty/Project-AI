apiVersion: v1
kind: Service
metadata:
  name: Constitutional Audit Service
  namespace: default
  labels:
    app: Constitutional Audit Service
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: http
    protocol: TCP
    name: http
  selector:
    app: Constitutional Audit Service
  sessionAffinity: None
