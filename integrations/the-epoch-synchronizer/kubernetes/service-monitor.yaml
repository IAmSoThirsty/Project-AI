apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: The Epoch Synchronizer
  namespace: default
  labels:
    app: The Epoch Synchronizer
    release: prometheus
spec:
  selector:
    matchLabels:
      app: The Epoch Synchronizer
  endpoints:
  - port: http
    path: /metrics
    interval: 30s
    scrapeTimeout: 10s
