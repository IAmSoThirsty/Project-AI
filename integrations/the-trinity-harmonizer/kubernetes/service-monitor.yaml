apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: The Trinity Harmonizer
  namespace: default
  labels:
    app: The Trinity Harmonizer
    release: prometheus
spec:
  selector:
    matchLabels:
      app: The Trinity Harmonizer
  endpoints:
  - port: http
    path: /metrics
    interval: 30s
    scrapeTimeout: 10s
