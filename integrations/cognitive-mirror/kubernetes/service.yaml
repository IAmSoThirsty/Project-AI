apiVersion: v1
kind: Service
metadata:
  name: Cognitive Mirror
  namespace: default
  labels:
    app: Cognitive Mirror
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: http
    protocol: TCP
    name: http
  selector:
    app: Cognitive Mirror
  sessionAffinity: None
