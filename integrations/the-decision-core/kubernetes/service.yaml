apiVersion: v1
kind: Service
metadata:
  name: The Decision Core
  namespace: default
  labels:
    app: The Decision Core
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: http
    protocol: TCP
    name: http
  selector:
    app: The Decision Core
  sessionAffinity: None
