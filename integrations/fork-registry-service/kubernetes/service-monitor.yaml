apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: Fork Registry Service
  namespace: default
  labels:
    app: Fork Registry Service
    release: prometheus
spec:
  selector:
    matchLabels:
      app: Fork Registry Service
  endpoints:
  - port: http
    path: /metrics
    interval: 30s
    scrapeTimeout: 10s
