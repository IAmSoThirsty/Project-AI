apiVersion: v1
kind: Service
metadata:
  name: Fork Registry Service
  namespace: default
  labels:
    app: Fork Registry Service
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: http
    protocol: TCP
    name: http
  selector:
    app: Fork Registry Service
  sessionAffinity: None
