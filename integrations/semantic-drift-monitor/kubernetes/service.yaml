apiVersion: v1
kind: Service
metadata:
  name: Semantic Drift Monitor
  namespace: default
  labels:
    app: Semantic Drift Monitor
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: http
    protocol: TCP
    name: http
  selector:
    app: Semantic Drift Monitor
  sessionAffinity: None
