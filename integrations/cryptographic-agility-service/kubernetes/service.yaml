apiVersion: v1
kind: Service
metadata:
  name: Cryptographic Agility Service
  namespace: default
  labels:
    app: Cryptographic Agility Service
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: http
    protocol: TCP
    name: http
  selector:
    app: Cryptographic Agility Service
  sessionAffinity: None
