apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: Temporal Anchor
  namespace: default
  labels:
    app: Temporal Anchor
    release: prometheus
spec:
  selector:
    matchLabels:
      app: Temporal Anchor
  endpoints:
  - port: http
    path: /metrics
    interval: 30s
    scrapeTimeout: 10s
