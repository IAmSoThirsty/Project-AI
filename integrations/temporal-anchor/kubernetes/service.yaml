apiVersion: v1
kind: Service
metadata:
  name: Temporal Anchor
  namespace: default
  labels:
    app: Temporal Anchor
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: http
    protocol: TCP
    name: http
  selector:
    app: Temporal Anchor
  sessionAffinity: None
