apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: EED memory Service
  namespace: default
  labels:
    app: EED memory Service
    release: prometheus
spec:
  selector:
    matchLabels:
      app: EED memory Service
  endpoints:
  - port: http
    path: /metrics
    interval: 30s
    scrapeTimeout: 10s
