apiVersion: v1
kind: Service
metadata:
  name: EED memory Service
  namespace: default
  labels:
    app: EED memory Service
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: http
    protocol: TCP
    name: http
  selector:
    app: EED memory Service
  sessionAffinity: None
