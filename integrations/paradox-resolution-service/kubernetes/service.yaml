apiVersion: v1
kind: Service
metadata:
  name: Paradox Resolution Service
  namespace: default
  labels:
    app: Paradox Resolution Service
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: http
    protocol: TCP
    name: http
  selector:
    app: Paradox Resolution Service
  sessionAffinity: None
