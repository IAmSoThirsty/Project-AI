apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: Paradox Resolution Service
  namespace: default
  labels:
    app: Paradox Resolution Service
    release: prometheus
spec:
  selector:
    matchLabels:
      app: Paradox Resolution Service
  endpoints:
  - port: http
    path: /metrics
    interval: 30s
    scrapeTimeout: 10s
