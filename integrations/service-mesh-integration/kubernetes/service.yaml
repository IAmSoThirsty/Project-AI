apiVersion: v1
kind: Service
metadata:
  name: Service mesh integration
  namespace: default
  labels:
    app: Service mesh integration
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: http
    protocol: TCP
    name: http
  selector:
    app: Service mesh integration
  sessionAffinity: None
