apiVersion: v1
kind: Service
metadata:
  name: Knowledge Decay Service
  namespace: default
  labels:
    app: Knowledge Decay Service
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: http
    protocol: TCP
    name: http
  selector:
    app: Knowledge Decay Service
  sessionAffinity: None
