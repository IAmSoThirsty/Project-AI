apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: Succession & Escrow Service
  namespace: default
  labels:
    app: Succession & Escrow Service
    release: prometheus
spec:
  selector:
    matchLabels:
      app: Succession & Escrow Service
  endpoints:
  - port: http
    path: /metrics
    interval: 30s
    scrapeTimeout: 10s
