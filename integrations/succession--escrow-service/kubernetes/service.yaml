apiVersion: v1
kind: Service
metadata:
  name: Succession & Escrow Service
  namespace: default
  labels:
    app: Succession & Escrow Service
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: http
    protocol: TCP
    name: http
  selector:
    app: Succession & Escrow Service
  sessionAffinity: None
