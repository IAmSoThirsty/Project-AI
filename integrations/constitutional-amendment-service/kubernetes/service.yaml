apiVersion: v1
kind: Service
metadata:
  name: Constitutional Amendment Service
  namespace: default
  labels:
    app: Constitutional Amendment Service
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: http
    protocol: TCP
    name: http
  selector:
    app: Constitutional Amendment Service
  sessionAffinity: None
