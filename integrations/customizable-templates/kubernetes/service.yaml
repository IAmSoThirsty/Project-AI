apiVersion: v1
kind: Service
metadata:
  name: Customizable templates
  namespace: default
  labels:
    app: Customizable templates
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: http
    protocol: TCP
    name: http
  selector:
    app: Customizable templates
  sessionAffinity: None
