apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: Sovereign Identity Service
  namespace: default
  labels:
    app: Sovereign Identity Service
    release: prometheus
spec:
  selector:
    matchLabels:
      app: Sovereign Identity Service
  endpoints:
  - port: http
    path: /metrics
    interval: 30s
    scrapeTimeout: 10s
