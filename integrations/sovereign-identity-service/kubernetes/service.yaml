apiVersion: v1
kind: Service
metadata:
  name: Sovereign Identity Service
  namespace: default
  labels:
    app: Sovereign Identity Service
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: http
    protocol: TCP
    name: http
  selector:
    app: Sovereign Identity Service
  sessionAffinity: None
