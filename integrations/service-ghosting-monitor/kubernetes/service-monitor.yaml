apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: Service Ghosting Monitor
  namespace: default
  labels:
    app: Service Ghosting Monitor
    release: prometheus
spec:
  selector:
    matchLabels:
      app: Service Ghosting Monitor
  endpoints:
  - port: http
    path: /metrics
    interval: 30s
    scrapeTimeout: 10s
