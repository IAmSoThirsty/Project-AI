apiVersion: v1
kind: Service
metadata:
  name: Service Ghosting Monitor
  namespace: default
  labels:
    app: Service Ghosting Monitor
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: http
    protocol: TCP
    name: http
  selector:
    app: Service Ghosting Monitor
  sessionAffinity: None
