apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: The Logic & Perception Layer
  namespace: default
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app: The Logic & Perception Layer
