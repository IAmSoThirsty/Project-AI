apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: The Logic & Perception Layer
  namespace: default
  labels:
    app: The Logic & Perception Layer
    release: prometheus
spec:
  selector:
    matchLabels:
      app: The Logic & Perception Layer
  endpoints:
  - port: http
    path: /metrics
    interval: 30s
    scrapeTimeout: 10s
