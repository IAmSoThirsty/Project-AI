apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: Semantic Firewall
  namespace: default
  labels:
    app: Semantic Firewall
    release: prometheus
spec:
  selector:
    matchLabels:
      app: Semantic Firewall
  endpoints:
  - port: http
    path: /metrics
    interval: 30s
    scrapeTimeout: 10s
