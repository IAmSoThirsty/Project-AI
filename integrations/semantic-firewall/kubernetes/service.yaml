apiVersion: v1
kind: Service
metadata:
  name: Semantic Firewall
  namespace: default
  labels:
    app: Semantic Firewall
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: http
    protocol: TCP
    name: http
  selector:
    app: Semantic Firewall
  sessionAffinity: None
