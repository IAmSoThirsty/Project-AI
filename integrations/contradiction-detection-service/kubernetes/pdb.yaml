apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: Contradiction Detection Service
  namespace: default
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app: Contradiction Detection Service
