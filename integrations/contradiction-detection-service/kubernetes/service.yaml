apiVersion: v1
kind: Service
metadata:
  name: Contradiction Detection Service
  namespace: default
  labels:
    app: Contradiction Detection Service
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: http
    protocol: TCP
    name: http
  selector:
    app: Contradiction Detection Service
  sessionAffinity: None
