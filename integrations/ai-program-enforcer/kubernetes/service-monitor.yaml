apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: AI Program Enforcer
  namespace: default
  labels:
    app: AI Program Enforcer
    release: prometheus
spec:
  selector:
    matchLabels:
      app: AI Program Enforcer
  endpoints:
  - port: http
    path: /metrics
    interval: 30s
    scrapeTimeout: 10s
