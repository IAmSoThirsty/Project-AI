apiVersion: v1
kind: Service
metadata:
  name: AI Program Enforcer
  namespace: default
  labels:
    app: AI Program Enforcer
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: http
    protocol: TCP
    name: http
  selector:
    app: AI Program Enforcer
  sessionAffinity: None
