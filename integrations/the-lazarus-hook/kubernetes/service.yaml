apiVersion: v1
kind: Service
metadata:
  name: The Lazarus Hook
  namespace: default
  labels:
    app: The Lazarus Hook
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: http
    protocol: TCP
    name: http
  selector:
    app: The Lazarus Hook
  sessionAffinity: None
