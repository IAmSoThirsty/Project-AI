apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: The Lazarus Hook
  namespace: default
  labels:
    app: The Lazarus Hook
    release: prometheus
spec:
  selector:
    matchLabels:
      app: The Lazarus Hook
  endpoints:
  - port: http
    path: /metrics
    interval: 30s
    scrapeTimeout: 10s
