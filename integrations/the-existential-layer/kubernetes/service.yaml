apiVersion: v1
kind: Service
metadata:
  name: The Existential Layer
  namespace: default
  labels:
    app: The Existential Layer
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: http
    protocol: TCP
    name: http
  selector:
    app: The Existential Layer
  sessionAffinity: None
