apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: The Existential Layer
  namespace: default
  labels:
    app: The Existential Layer
    release: prometheus
spec:
  selector:
    matchLabels:
      app: The Existential Layer
  endpoints:
  - port: http
    path: /metrics
    interval: 30s
    scrapeTimeout: 10s
