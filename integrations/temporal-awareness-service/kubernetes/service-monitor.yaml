apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: Temporal Awareness Service
  namespace: default
  labels:
    app: Temporal Awareness Service
    release: prometheus
spec:
  selector:
    matchLabels:
      app: Temporal Awareness Service
  endpoints:
  - port: http
    path: /metrics
    interval: 30s
    scrapeTimeout: 10s
