apiVersion: v1
kind: Service
metadata:
  name: Zero-downtime deploys
  namespace: default
  labels:
    app: Zero-downtime deploys
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: http
    protocol: TCP
    name: http
  selector:
    app: Zero-downtime deploys
  sessionAffinity: None
