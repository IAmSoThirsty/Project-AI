# Project-AI Desktop

Unsigned development PyQt6 operator application for gateway status, canonical
replay evidence checks, verified audit viewing, unverified capability claim
inspection, governance display, and in-memory settings.

The desktop application does not import governance or execution packages. It
does not persist API tokens. Capability inspection decodes public claims but
cannot authenticate signatures without issuer authority, and labels that limit
in the UI.
